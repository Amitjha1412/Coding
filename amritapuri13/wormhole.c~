#include<stdio.h>
#include<stdlib.h>

inline void Scan_f(lli *a)
{
    char c = 0;
    while(c<33)
        //c = fgetc_unlocked(stdin);
        c = getc(stdin);
    *a = 0;
    while(c>33)
    {
        *a = *a*10 + c - '0';
        //c = fgetc_unlocked(stdin);
        c = getc(stdin);
    }
}

int cmpfunc (const void *a,const void *b)
{
    lli i=*(lli *)a;
    lli j=*(lli *)b;
    if(j>=i)
        return 1;
    return 0;
}

int cmpfunc
int main()
{

}
